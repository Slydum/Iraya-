{
  "name": "iraya",
  "private": true,
  "version": "0.4.0",
  "type": "module",
  "scripts": {
    "assets": "node tools/materialize_runtime_assets.mjs && node tools/materialize_modern_farm_code.mjs && node tools/materialize_modern_farm_runtime.mjs",
    "dev": "npm run assets && vite --host 0.0.0.0",
    "build": "npm run assets && tsc -p tsconfig.json && vite build",
    "preview": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vite": "^8.1.5"
  }
}

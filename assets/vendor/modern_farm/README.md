# Modern Farm local asset directory

Run `python tools/import_modern_farm.py /path/to/Modern_Farm_v1.2.zip` from the repository root.

The extracted source art is ignored by Git because the asset license prohibits redistribution. Keep this repository private and never publish the original pack files.
